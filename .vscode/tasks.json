{
	"version": "2.0.0",
	"tasks": [
		{
		  "type": "shell",
		  "label": "startDevServer",
		  "command": "yarn",
		  "args": [
			"run",
			"serve"
		  ],
		  "options": {
			"cwd": "${workspaceFolder}"
		  },
		  "problemMatcher": []
		}
	  ]
}